import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maxqwars.sayan',
  appName: 'Sayan',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
